[build]
  publish = "."
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

[functions]
  node_bundler = "esbuild"

# (선택) 캐시/보안 헤더 최소
[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "no-store"
